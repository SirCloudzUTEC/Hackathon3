# Base de la API. Incluye el prefijo /api/v1.
# - Desarrollo local (npm run dev)  -> ver .env            (localhost:8080)
# - Build de producción (vite build) -> ver .env.production (Cloud Run)
VITE_API_BASE_URL=https://hackaton-20261-front-587720740455.us-east1.run.app/api/v1
